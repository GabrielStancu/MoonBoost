<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MoonBoost.MainPage">

    <StackLayout>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" 
                    Text="NA" 
                    Clicked="OnNewActivityClicked"/>
            <Picker Grid.Column="1"
                    ItemsSource="{Binding Plans}"
                    SelectedItem="{Binding SelectedPlan}"
                    ItemDisplayBinding="{Binding Name}">
            </Picker>
            <Button Grid.Column="2" 
                    Text="NP" 
                    Clicked="OnNewPlanClicked"/>
        </Grid>

        <ScrollView>
            <ListView ItemsSource="{Binding ToDos}" HasUnevenRows="True" ItemSelected="OnSelectedPost" SelectedItem="{Binding SelectedToDo}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding Completed}"/>
                                    <Label Text="{Binding Activity.Title}"/>
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollView>
    </StackLayout>
</ContentPage>
